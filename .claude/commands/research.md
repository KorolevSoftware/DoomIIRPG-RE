---
description: Investigate a hypothesis about the original code (src/) and document the findings.
argument-hint: <hypothesis or question about src/>
---

Delegate to the `researcher` agent (Agent tool, `subagent_type: researcher`) — launch several in parallel if the question splits into independent parts.

Investigate the following hypothesis/question about the original Doom II RPG code and follow the researcher output protocol (report + `docs/original-code/` topic update + `docs/research/` log entry):

$ARGUMENTS

Then summarize the verdict and the doc paths for the user.
